---
phase: 01-foundation-tech-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs, tsconfig.json, src/env.d.ts, .gitignore]
autonomous: true
---

<objective>
Initialize Astro project with blog template and essential dependencies.

Purpose: Establish the foundation for a content-first blog using Astro 5.0, optimized for effortless writing experience.
Output: Working Astro project with TypeScript, blog template structure, and SEO packages installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tech-stack/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with blog template</name>
  <files>package.json, astro.config.mjs, tsconfig.json, src/, public/</files>
  <action>Run `npm create astro@latest . -- --template blog --typescript strict --install --git` to initialize Astro project in current directory with blog template, TypeScript strict mode, and automatic dependency installation. Use blog template (not minimal) to get content structure out of the box. Accept defaults for remaining prompts.</action>
  <verify>Verify package.json exists with astro dependency, astro.config.mjs exists, tsconfig.json has strict mode, src/ directory has blog template structure</verify>
  <done>Astro project initialized with blog template, TypeScript strict mode enabled, dependencies installed, src/content/blog/ exists with sample posts</done>
</task>

<task type="auto">
  <name>Task 2: Install SEO and sitemap packages</name>
  <files>package.json</files>
  <action>Install essential SEO packages: `npm install @astrojs/sitemap astro-seo`. These provide automatic sitemap generation and SEO meta tag helpers. Do NOT install astro-seo-schema or astro-robots-txt yet (defer to Phase 5). Focus on core dependencies only.</action>
  <verify>Run `npm list @astrojs/sitemap astro-seo` to confirm both packages installed</verify>
  <done>@astrojs/sitemap and astro-seo packages installed and listed in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Configure Astro with site URL and sitemap integration</name>
  <files>astro.config.mjs</files>
  <action>Update astro.config.mjs to add site URL and sitemap integration. Import sitemap, set site to 'https://yourdomain.com' (placeholder), add sitemap to integrations array. Configuration format:

```javascript
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://yourdomain.com', // TODO: Update with actual domain
  integrations: [sitemap()],
});
```

This enables automatic sitemap.xml generation for SEO. Actual domain will be configured during deployment.</action>
  <verify>Run `cat astro.config.mjs` to confirm site and sitemap integration present</verify>
  <done>astro.config.mjs configured with site URL placeholder and sitemap integration, ready for sitemap.xml generation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts development server successfully
- [ ] astro.config.mjs has site and sitemap configured
- [ ] TypeScript strict mode enabled in tsconfig.json
- [ ] src/content/blog/ directory exists with sample content
</verification>

<success_criteria>

- All tasks completed
- Astro project builds successfully
- Blog template structure in place
- SEO packages installed
- Ready for Content Collections configuration
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tech-stack/01-01-SUMMARY.md`
</output>
